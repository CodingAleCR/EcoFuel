<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="info.codingalecr.refuel">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="info.codingalecr.refuel.ReFuelApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="info.codingalecr.refuel.views.activities.MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="info.codingalecr.refuel.views.activities.FuelListActivity"
            android:label="@string/refuels_title"
            android:parentActivityName="info.codingalecr.refuel.views.activities.MenuActivity">
            <meta-allItems
                android:name="android.support.PARENT_ACTIVITY"
                android:value="info.codingalecr.refuel.views.activities.MenuActivity"/>
        </activity>
        <activity
            android:name="info.codingalecr.refuel.views.activities.RefuelDetailActivity"
            android:label="@string/refuels_title"
            android:parentActivityName="info.codingalecr.refuel.views.activities.FuelListActivity">
            <meta-allItems
                android:name="android.support.PARENT_ACTIVITY"
                android:value="info.codingalecr.refuel.views.activities.FuelListActivity"/>
        </activity>
        <activity
            android:name="info.codingalecr.refuel.views.activities.FuelItemFormActivity"
            android:label="@string/add_refuel"
            android:parentActivityName="info.codingalecr.refuel.views.activities.FuelListActivity">
            <meta-allItems
                android:name="android.support.PARENT_ACTIVITY"
                android:value="info.codingalecr.refuel.views.activities.FuelListActivity"/>
        </activity>
        <activity
            android:name="info.codingalecr.refuel.views.activities.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="info.codingalecr.refuel.views.activities.MenuActivity">
            <meta-allItems
                android:name="android.support.PARENT_ACTIVITY"
                android:value="info.codingalecr.refuel.views.activities.MenuActivity"/>
        </activity>
    </application>

</manifest>